[pytest]
testpaths = tests
python_files = test_*.py
addopts = -q --strict-markers
markers =
    live: test requires a real Groq API key and network access
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
